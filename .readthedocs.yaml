version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - curl -LsSf https://astral.sh/uv/install.sh | sh && export PATH=$HOME/.cargo/bin:$PATH && make doc

mkdocs:
  configuration: mkdocs.yml
