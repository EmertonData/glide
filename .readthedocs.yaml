version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - uv sync --group doc
    - uv run mkdocs build --config-file mkdocs.yml

mkdocs:
  configuration: mkdocs.yml
